/* se usara el modulo de bloque para crear la cadena de bloques */
const Block = require('../blocks/block');
class Blockchain{
  constructior(){
    /* pendiente */
  }
}

module.exports = Blockchain;